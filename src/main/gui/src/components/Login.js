import React, { Component } from "react";
import { Button } from '@material-ui/core';

class Login extends Component {
    render() {
        return <Button variant='contained' color='primary'>ABCD</Button>
    }
}

export default Login;